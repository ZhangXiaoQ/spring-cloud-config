server:
  port: 8088
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          # 配置git仓库地址
          uri: https://github.com/ZhangXiaoQ/spring-cloud-config.git

